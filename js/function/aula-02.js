// Passando parâmetros e dados para a função 

function sum(n1, n2) { 
    let result = n1 + n2;
    console.log(`Result ${result}`);
    
}

sum(10, 15); //Essa função está trabalhando com parâmetros

function fullName (name, lastName){
    console.log(`Olá, ${name} ${lastName}`);
}

fullName('Lucas', 'Sousa');